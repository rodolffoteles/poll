version: '3'
services:
  app:
    build: ./app
    restart: always
    ports:
      - '8080:8080'
    env_file:
      - ./app/.env
    links:
      - db
  db:
    build: ./db
    ports:
      - '3306:3306'
    env_file:
      - ./db/.env
